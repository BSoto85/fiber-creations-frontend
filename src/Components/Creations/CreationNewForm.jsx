import { useState } from "react";
import { useNavigate, Link } from "react-router-dom";

const URL = import.meta.env.VITE_BASE_URL;

const CreationNewForm = ({ user }) => {
  return <div>CreationNewForm</div>;
};

export default CreationNewForm;
